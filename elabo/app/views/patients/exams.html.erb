<h1>Liste des examens pour <%= @laboratory.name %></h1>

<ul>
  <% @exams.each do |exam| %>
    <li>
      <%= exam.name %> - <%= number_to_currency(exam.price) %>
      <%= link_to 'Réserver cet examen', new_appointment_path(exam_id: exam.id) %>
    </li>
  <% end %>
</ul>
